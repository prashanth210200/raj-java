package Collectors;

public class GroupingElements {

	public static void main(String[] args) {
		/*
		 * Grouping elements:

			groupingBy(classifier): Groups the stream elements into a Map based on the provided classifier function.
			groupingBy(classifier, downstream): Groups the stream elements into a Map based on the classifier function 
			and applies a downstream collector to each group.
			partitioningBy(predicate): Partitions the stream elements into two groups based on the given predicate.
		 */

	}

}
