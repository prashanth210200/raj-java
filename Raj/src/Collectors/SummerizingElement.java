package Collectors;

public class SummerizingElement {

	public static void main(String[] args) {
		/*
		 * Summarizing elements:

		counting(): Returns the count of elements in the stream.
		summingInt(), summingLong(), summingDouble(): Returns the sum of a numeric property of the stream elements.
		averagingInt(), averagingLong(), averagingDouble(): Returns the average of a numeric property of the stream elements.
		minBy(comparator), maxBy(comparator): Returns the minimum or maximum element of the stream based on the provided comparator.
		 */

	}

}
